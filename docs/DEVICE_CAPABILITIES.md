# DEVICE_CAPABILITIES

## Windows

The current workspace is a Windows environment; local Python and Tkinter GUI support are verified. Windows-authorized device automation is only partially implemented and remains PASS/blocked pending explicit authorization and permission policy.

## Android

No ADB or Android device bridge is available in the workspace environment. The status is `OPTIONAL_NOT_INSTALLED` or `BLOCKED`.

## Multi-device

Device abstraction is architecturally available as a registry/provider pattern, but no concrete device provider is authorized for testing.
