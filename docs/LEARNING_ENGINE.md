# Learning Engine

Status: `MISSING` as a connected runtime capability. Existing self-improvement records capture experiment metadata, but ordinary conversations do not extract lessons, user corrections, or successful strategies.

Required next boundary: write typed lesson records after verified tasks, rank similar lessons by topic, and allow explicit correction/forget commands. No unverified model inference should be stored as fact.
